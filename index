import { useState, useEffect, useRef } from "react";

// ─── CSS injected globally ───────────────────────────────────────────────────
const globalCSS = `
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');

  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

  :root {
    --navy:      #050d1a;
    --navy-2:    #091428;
    --navy-3:    #0e2044;
    --blue:      #0e71eb;
    --blue-2:    #2d8cff;
    --cyan:      #00c2e0;
    --cyan-2:    #6ee7f7;
    --white:     #f0f6ff;
    --muted:     #7a93b8;
    --border:    rgba(46,140,255,.18);
    --glow:      rgba(0,194,224,.35);
    --font-head: 'Inter', sans-serif;
    --font-body: 'Inter', sans-serif;
  }

  html { scroll-behavior: smooth; }

  body {
    background: var(--navy);
    color: var(--white);
    font-family: var(--font-body);
    overflow-x: hidden;
    line-height: 1.6;
  }

  /* Scrollbar */
  ::-webkit-scrollbar { width: 5px; }
  ::-webkit-scrollbar-track { background: var(--navy-2); }
  ::-webkit-scrollbar-thumb { background: var(--blue); border-radius: 3px; }

  /* Keyframes */
  @keyframes floatY {
    0%,100% { transform: translateY(0); }
    50%      { transform: translateY(-14px); }
  }
  @keyframes pulseRing {
    0%   { transform: scale(.9); opacity:.7; }
    70%  { transform: scale(1.4); opacity:0; }
    100% { transform: scale(.9); opacity:0; }
  }
  @keyframes rotateSlow {
    to { transform: rotate(360deg); }
  }
  @keyframes shimmer {
    0%   { background-position: -200% center; }
    100% { background-position:  200% center; }
  }
  @keyframes fadeUp {
    from { opacity:0; transform:translateY(32px); }
    to   { opacity:1; transform:translateY(0); }
  }
  @keyframes blinkCaret {
    0%,100% { opacity:1; }
    50%     { opacity:0; }
  }
  @keyframes orbit {
    to { transform: rotate(360deg) translateX(90px) rotate(-360deg); }
  }
  @keyframes gradMove {
    0%,100% { background-position: 0% 50%; }
    50%      { background-position: 100% 50%; }
  }
  @keyframes waveBar {
    0%,100% { transform:scaleY(1); }
    50%     { transform:scaleY(2.2); }
  }

  .fade-up   { animation: fadeUp .7s ease both; }
  .float     { animation: floatY 4s ease-in-out infinite; }
  .rotate    { animation: rotateSlow 18s linear infinite; }
  .grad-text {
    background: linear-gradient(90deg, var(--cyan-2), var(--blue-2), var(--cyan));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }
  .shimmer-text {
    background: linear-gradient(90deg, var(--cyan-2) 25%, #fff 50%, var(--cyan-2) 75%);
    background-size: 200% auto;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: shimmer 3s linear infinite;
  }

  /* Glass card */
  .glass {
    background: rgba(9,20,40,.72);
    border: 1px solid var(--border);
    backdrop-filter: blur(14px);
    border-radius: 18px;
  }

  /* Glow button */
  .btn-primary {
    display:inline-flex; align-items:center; gap:8px;
    background: linear-gradient(135deg, var(--blue) 0%, var(--cyan) 100%);
    color:#fff; font-family:var(--font-head); font-weight:700;
    font-size:1rem; letter-spacing:.03em;
    padding:14px 32px; border-radius:50px; border:none; cursor:pointer;
    box-shadow: 0 0 28px rgba(0,194,224,.35);
    transition: transform .2s, box-shadow .2s;
    text-decoration:none;
    position:relative; overflow:hidden;
  }
  .btn-primary::after {
    content:''; position:absolute; inset:0;
    background:linear-gradient(135deg,rgba(255,255,255,.12),transparent);
    opacity:0; transition:opacity .2s;
  }
  .btn-primary:hover { transform:translateY(-2px) scale(1.03); box-shadow:0 0 44px rgba(0,194,224,.55); }
  .btn-primary:hover::after { opacity:1; }

  .btn-ghost {
    display:inline-flex; align-items:center; gap:8px;
    background:transparent; border:1.5px solid var(--border);
    color:var(--white); font-family:var(--font-head); font-weight:600;
    font-size:.95rem; padding:13px 28px; border-radius:50px; cursor:pointer;
    transition:border-color .2s, color .2s, background .2s;
    text-decoration:none;
  }
  .btn-ghost:hover { border-color:var(--cyan); color:var(--cyan); background:rgba(0,194,224,.07); }

  /* Section labels */
  .section-tag {
    display:inline-flex; align-items:center; gap:6px;
    font-size:.75rem; font-weight:700; letter-spacing:.12em; text-transform:uppercase;
    color:var(--cyan); background:rgba(0,194,224,.1);
    border:1px solid rgba(0,194,224,.25);
    padding:5px 14px; border-radius:50px;
    margin-bottom:1.25rem;
  }
  .section-tag::before {
    content:''; width:6px; height:6px; border-radius:50%;
    background:var(--cyan); box-shadow:0 0 8px var(--cyan);
  }

  /* Noise overlay */
  .noise::before {
    content:''; position:fixed; inset:0; pointer-events:none; z-index:0;
    opacity:.025;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  }

  /* Grid background */
  .grid-bg {
    background-image:
      linear-gradient(var(--border) 1px, transparent 1px),
      linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 48px 48px;
  }

  /* Divider */
  .divider {
    height:1px;
    background:linear-gradient(90deg,transparent,var(--border),transparent);
    margin:0;
  }

  /* Stat number */
  .stat-num {
    font-family:var(--font-head); font-size:3rem; font-weight:800; line-height:1;
    background:linear-gradient(135deg,#fff,var(--cyan-2));
    -webkit-background-clip:text; -webkit-text-fill-color:transparent; background-clip:text;
  }

  /* Feature card hover */
  .feat-card {
    transition: transform .25s, box-shadow .25s, border-color .25s;
  }
  .feat-card:hover {
    transform:translateY(-6px);
    box-shadow:0 24px 60px rgba(0,194,224,.12);
    border-color:rgba(0,194,224,.4);
  }

  /* Nav link */
  .nav-link {
    font-size:.9rem; font-weight:500; color:var(--muted);
    text-decoration:none; transition:color .2s; padding:4px 0;
    position:relative;
  }
  .nav-link::after {
    content:''; position:absolute; bottom:-2px; left:0; width:0; height:1.5px;
    background:var(--cyan); transition:width .25s;
  }
  .nav-link:hover { color:var(--white); }
  .nav-link:hover::after { width:100%; }

  /* Wave bars */
  .wave-bar { display:inline-block; width:3px; background:var(--cyan); border-radius:2px; margin:0 1.5px; }

  /* Responsive */
  @media (max-width:768px) {
    .hero-grid { flex-direction:column !important; }
    .hide-mobile { display:none !important; }
    .stat-num { font-size:2.2rem; }
  }
`;

// ─── Utility Components ──────────────────────────────────────────────────────

function InjectCSS() {
  useEffect(() => {
    const style = document.createElement("style");
    style.textContent = globalCSS;
    document.head.appendChild(style);
    return () => document.head.removeChild(style);
  }, []);
  return null;
}

function useInView(threshold = 0.15) {
  const ref = useRef(null);
  const [visible, setVisible] = useState(false);
  useEffect(() => {
    const obs = new IntersectionObserver(([e]) => { if (e.isIntersecting) setVisible(true); }, { threshold });
    if (ref.current) obs.observe(ref.current);
    return () => obs.disconnect();
  }, [threshold]);
  return [ref, visible];
}

function AnimSection({ children, delay = 0, style = {} }) {
  const [ref, visible] = useInView();
  return (
    <div ref={ref} style={{ opacity: visible ? 1 : 0, transform: visible ? "translateY(0)" : "translateY(36px)", transition: `opacity .7s ease ${delay}s, transform .7s ease ${delay}s`, ...style }}>
      {children}
    </div>
  );
}

// ─── Icons (inline SVG) ──────────────────────────────────────────────────────
const Icon = {
  Phone:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" strokeLinejoin="round" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/></svg>,
  Clock:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><circle cx="12" cy="12" r="10"/><path strokeLinecap="round" d="M12 6v6l4 2"/></svg>,
  Users:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M17 21v-2a4 4 0 00-4-4H5a4 4 0 00-4 4v2"/><circle cx="9" cy="7" r="4"/><path strokeLinecap="round" d="M23 21v-2a4 4 0 00-3-3.87M16 3.13a4 4 0 010 7.75"/></svg>,
  Bot:      () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><rect x="3" y="11" width="18" height="11" rx="2"/><path strokeLinecap="round" d="M12 11V5m0 0a2 2 0 100-4 2 2 0 000 4zM8 15h.01M16 15h.01"/></svg>,
  Mic:      () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M12 1a3 3 0 00-3 3v8a3 3 0 006 0V4a3 3 0 00-3-3z"/><path strokeLinecap="round" d="M19 10v2a7 7 0 01-14 0v-2M12 19v4m-4 0h8"/></svg>,
  Globe:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><circle cx="12" cy="12" r="10"/><path strokeLinecap="round" d="M2 12h20M12 2a15.3 15.3 0 010 20M12 2a15.3 15.3 0 000 20"/></svg>,
  Zap:      () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" strokeLinejoin="round" d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>,
  Chart:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/></svg>,
  Dollar:   () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M12 1v22M17 5H9.5a3.5 3.5 0 000 7h5a3.5 3.5 0 010 7H6"/></svg>,
  Smile:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><circle cx="12" cy="12" r="10"/><path strokeLinecap="round" d="M8 14s1.5 2 4 2 4-2 4-2M9 9h.01M15 9h.01"/></svg>,
  Scale:    () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M3 6l3 1m0 0l-3 9a5.002 5.002 0 006.001 0M6 7l3 9M6 7l6-2m6 2l3-1m-3 1l-3 9a5.002 5.002 0 006.001 0M18 7l3 9m-3-9l-6-2m0-2v2m0 16V5m0 16H9m3 0h3"/></svg>,
  Headset:  () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M3 18v-6a9 9 0 0118 0v6"/><path strokeLinecap="round" d="M21 19a2 2 0 01-2 2h-1a2 2 0 01-2-2v-3a2 2 0 012-2h3zM3 19a2 2 0 002 2h1a2 2 0 002-2v-3a2 2 0 00-2-2H3z"/></svg>,
  Calendar: () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><rect x="3" y="4" width="18" height="18" rx="2"/><path strokeLinecap="round" d="M16 2v4M8 2v4M3 10h18"/></svg>,
  HelpCircle:() => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><circle cx="12" cy="12" r="10"/><path strokeLinecap="round" d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3M12 17h.01"/></svg>,
  Target:   () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="6"/><circle cx="12" cy="12" r="2"/></svg>,
  Check:    () => <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2.5"><path strokeLinecap="round" strokeLinejoin="round" d="M5 13l4 4L19 7"/></svg>,
  Arrow:    () => <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3"/></svg>,
  Mail:     () => <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/></svg>,
  Rocket:   () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" strokeLinejoin="round" d="M9 11l3-3m0 0l3 3m-3-3v8m0-13a2 2 0 110 4 2 2 0 010-4zm-7 4a9 9 0 1118 0c0 5.25-4 10.5-9 11.5C5 20.5 2 16.25 2 11z"/></svg>,
  Shield:   () => <svg width="22" height="22" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth="1.8"><path strokeLinecap="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>,
  Linkedin: () => <svg width="18" height="18" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>,
  Twitter:  () => <svg width="18" height="18" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>,
};

// ─── Navbar ───────────────────────────────────────────────────────────────────
function Navbar() {
  const [scrolled, setScrolled] = useState(false);
  const [open, setOpen] = useState(false);
  useEffect(() => {
    const fn = () => setScrolled(window.scrollY > 40);
    window.addEventListener("scroll", fn);
    return () => window.removeEventListener("scroll", fn);
  }, []);

  const links = [
    { label: "Solutions", href: "#solution" },
    { label: "Features", href: "#features" },
    { label: "Benefits", href: "#benefits" },
    { label: "Use Cases", href: "#usecases" },
    { label: "Why Us", href: "#why" },
  ];

  return (
    <nav style={{
      position: "fixed", top: 0, left: 0, right: 0, zIndex: 100,
      padding: scrolled ? "12px 0" : "20px 0",
      background: scrolled ? "rgba(5,13,26,.92)" : "transparent",
      backdropFilter: scrolled ? "blur(20px)" : "none",
      borderBottom: scrolled ? "1px solid var(--border)" : "none",
      transition: "all .35s ease",
    }}>
      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px", display: "flex", alignItems: "center", justifyContent: "space-between" }}>
        {/* Logo */}
        <a href="#" style={{ display: "flex", alignItems: "center", gap: 10, textDecoration: "none" }}>
          <div style={{
            width: 36, height: 36, borderRadius: 10,
            background: "linear-gradient(135deg, var(--blue), var(--cyan))",
            display: "flex", alignItems: "center", justifyContent: "center",
            boxShadow: "0 0 18px rgba(0,194,224,.4)",
          }}>
            <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="white" strokeWidth="2">
              <path strokeLinecap="round" strokeLinejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/>
            </svg>
          </div>
          <span style={{ fontFamily: "var(--font-head)", fontWeight: 800, fontSize: "1.2rem", color: "var(--white)", letterSpacing: "-.01em" }}>
            Sys<span className="grad-text">grate</span>
          </span>
        </a>

        {/* Desktop links */}
        <div className="hide-mobile" style={{ display: "flex", gap: 32 }}>
          {links.map(l => <a key={l.href} href={l.href} className="nav-link">{l.label}</a>)}
        </div>

        {/* CTA */}
        <a href="#cta" className="btn-primary hide-mobile" style={{ padding: "10px 24px", fontSize: ".88rem" }}>
          Book a Demo <Icon.Arrow />
        </a>

        {/* Hamburger */}
        <button
          onClick={() => setOpen(!open)}
          style={{ background: "none", border: "1.5px solid var(--border)", borderRadius: 8, color: "var(--white)", padding: "6px 10px", cursor: "pointer", display: "none" }}
          className="show-mobile"
        >☰</button>
      </div>

      {/* Mobile menu */}
      {open && (
        <div style={{ background: "rgba(5,13,26,.97)", padding: "16px 32px 24px", borderTop: "1px solid var(--border)" }}>
          {links.map(l => (
            <a key={l.href} href={l.href} onClick={() => setOpen(false)}
               style={{ display: "block", padding: "12px 0", color: "var(--white)", textDecoration: "none", borderBottom: "1px solid var(--border)", fontFamily: "var(--font-body)" }}>
              {l.label}
            </a>
          ))}
          <a href="#cta" className="btn-primary" style={{ marginTop: 16, display: "inline-flex" }}>Book a Demo</a>
        </div>
      )}
    </nav>
  );
}

// ─── Hero ─────────────────────────────────────────────────────────────────────
function Hero() {
  return (
    <section style={{ minHeight: "100vh", display: "flex", alignItems: "center", position: "relative", overflow: "hidden", paddingTop: 100 }}>
      {/* Mesh gradient BG */}
      <div style={{
        position: "absolute", inset: 0, zIndex: 0,
        background: `
          radial-gradient(ellipse 80% 60% at 60% 40%, rgba(14,113,235,.22) 0%, transparent 70%),
          radial-gradient(ellipse 50% 50% at 20% 80%, rgba(0,194,224,.12) 0%, transparent 60%),
          var(--navy)
        `,
      }}/>
      {/* Grid */}
      <div className="grid-bg" style={{ position: "absolute", inset: 0, zIndex: 0, opacity: .4 }}/>

      {/* Floating orb */}
      <div className="float" style={{
        position: "absolute", top: "18%", right: "12%", width: 360, height: 360, borderRadius: "50%",
        background: "radial-gradient(circle, rgba(0,194,224,.18) 0%, transparent 70%)",
        filter: "blur(40px)", zIndex: 0,
      }}/>

      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px", position: "relative", zIndex: 1, width: "100%" }}>
        <div className="hero-grid" style={{ display: "flex", alignItems: "center", gap: 60 }}>
          {/* Left content */}
          <div style={{ flex: 1, minWidth: 0 }}>
            {/* Zoom badge */}
            <div style={{ display: "inline-flex", alignItems: "center", gap: 10, background: "rgba(14,113,235,.15)", border: "1px solid rgba(14,113,235,.3)", borderRadius: 50, padding: "7px 18px", marginBottom: 28 }}>
              <div style={{ width: 22, height: 22, background: "linear-gradient(135deg,#0e71eb,#2d8cff)", borderRadius: 6, display: "flex", alignItems: "center", justifyContent: "center" }}>
                <svg width="12" height="12" fill="white" viewBox="0 0 24 24"><path d="M24 12c0 6.627-5.373 12-12 12S0 18.627 0 12 5.373 0 12 0s12 5.373 12 12z"/><path fill="rgba(255,255,255,.6)" d="M7 8.5v7l4-2.5-4-4.5zm4 2l5 3-5 3V10.5z"/></svg>
              </div>
              <span style={{ fontSize: ".78rem", fontWeight: 700, letterSpacing: ".08em", color: "var(--blue-2)", textTransform: "uppercase" }}>
                Zoom Virtual Agent Partner
              </span>
            </div>

            <h1 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2.4rem,5vw,4.2rem)", fontWeight: 800, lineHeight: 1.08, letterSpacing: "-.03em", marginBottom: 24 }}>
              Transform Customer<br/>
              <span className="shimmer-text">Experience with</span><br/>
              Zoom Virtual Agent
            </h1>

            <p style={{ fontSize: "1.15rem", color: "var(--muted)", lineHeight: 1.7, maxWidth: 520, marginBottom: 40 }}>
              <strong style={{ color: "var(--white)" }}>Powered by Sysgrate Integration Services.</strong> Deploy AI-driven chat and voice automation that works 24/7 — reducing costs, delighting customers, and scaling effortlessly.
            </p>

            <div style={{ display: "flex", gap: 16, flexWrap: "wrap" }}>
              <a href="#cta" className="btn-primary" style={{ fontSize: "1.05rem", padding: "15px 36px" }}>
                Book a Demo <Icon.Arrow />
              </a>
              <a href="#solution" className="btn-ghost">
                See How It Works
              </a>
            </div>

            {/* Stats row */}
            <div style={{ display: "flex", gap: 40, marginTop: 52, flexWrap: "wrap" }}>
              {[["40%", "Cost Reduction"], ["3×", "Faster Response"], ["98%", "CSAT Score"]].map(([num, label]) => (
                <div key={label}>
                  <div className="stat-num">{num}</div>
                  <div style={{ fontSize: ".8rem", color: "var(--muted)", marginTop: 2, fontWeight: 500 }}>{label}</div>
                </div>
              ))}
            </div>
          </div>

          {/* Right: AI illustration */}
          <div className="float hide-mobile" style={{ flex: "0 0 480px", position: "relative" }}>
            <AIIllustration />
          </div>
        </div>
      </div>

      {/* Bottom fade */}
      <div style={{ position: "absolute", bottom: 0, left: 0, right: 0, height: 120, background: "linear-gradient(to bottom, transparent, var(--navy))", zIndex: 1 }}/>
    </section>
  );
}

function AIIllustration() {
  return (
    <div style={{ position: "relative", width: 480, height: 480 }}>
      {/* Outer ring */}
      <div className="rotate" style={{
        position: "absolute", inset: 20, borderRadius: "50%",
        border: "1px dashed rgba(0,194,224,.25)",
      }}/>
      {/* Middle ring */}
      <div style={{
        position: "absolute", inset: 80, borderRadius: "50%",
        border: "1.5px solid rgba(14,113,235,.3)",
        boxShadow: "0 0 40px rgba(0,194,224,.08) inset",
      }}/>

      {/* Orbiting nodes */}
      {[
        { icon: <Icon.Bot />,  angle: 0,   label: "AI Chat",    color: "#0e71eb" },
        { icon: <Icon.Mic />,  angle: 120, label: "Voice AI",   color: "#00c2e0" },
        { icon: <Icon.Chart />,angle: 240, label: "Analytics",  color: "#6ee7f7" },
      ].map(({ icon, angle, label, color }) => {
        const rad = (angle * Math.PI) / 180;
        const r = 160;
        const cx = 240 + r * Math.cos(rad);
        const cy = 240 + r * Math.sin(rad);
        return (
          <div key={label} style={{
            position: "absolute",
            left: cx - 30, top: cy - 30,
            width: 60, height: 60,
            borderRadius: "50%",
            background: `rgba(${color === "#0e71eb" ? "14,113,235" : color === "#00c2e0" ? "0,194,224" : "110,231,247"},.15)`,
            border: `1.5px solid ${color}44`,
            display: "flex", alignItems: "center", justifyContent: "center",
            color,
            boxShadow: `0 0 20px ${color}33`,
          }}>
            {icon}
          </div>
        );
      })}

      {/* Center card */}
      <div className="glass" style={{
        position: "absolute", left: "50%", top: "50%",
        transform: "translate(-50%,-50%)",
        width: 200, height: 200, borderRadius: "50%",
        display: "flex", flexDirection: "column",
        alignItems: "center", justifyContent: "center",
        gap: 10,
        boxShadow: "0 0 80px rgba(0,194,224,.2), 0 0 160px rgba(14,113,235,.1)",
        border: "1.5px solid rgba(0,194,224,.3)",
        background: "rgba(9,20,40,.9)",
      }}>
        {/* Zoom logo placeholder */}
        <div style={{
          width: 56, height: 56,
          background: "linear-gradient(135deg,#0e71eb,#2d8cff)",
          borderRadius: 16,
          display: "flex", alignItems: "center", justifyContent: "center",
          boxShadow: "0 0 24px rgba(14,113,235,.6)",
        }}>
          <svg width="28" height="28" fill="white" viewBox="0 0 80 80">
            <rect width="80" height="80" rx="18" fill="none"/>
            <path d="M20 28h24a6 6 0 016 6v14a2 2 0 01-2 2H22a6 6 0 01-6-6V30a2 2 0 012-2z"/>
            <path d="M50 38l16-10v24L50 42v-4z" opacity=".85"/>
          </svg>
        </div>
        <div style={{ fontFamily: "var(--font-head)", fontWeight: 700, fontSize: ".95rem", color: "var(--white)", textAlign: "center", lineHeight: 1.3 }}>
          Zoom<br/>Virtual Agent
        </div>
        {/* Wave bars */}
        <div style={{ display: "flex", alignItems: "center", height: 20 }}>
          {[1, 1.6, 2.3, 1.8, 1.2, 2, 1.4].map((h, i) => (
            <div key={i} className="wave-bar" style={{
              height: `${h * 8}px`,
              background: `hsl(${190 + i * 5},100%,65%)`,
              animationName: "waveBar",
              animationDuration: `${0.5 + i * 0.1}s`,
              animationTimingFunction: "ease-in-out",
              animationIterationCount: "infinite",
              animationDelay: `${i * 0.08}s`,
            }}/>
          ))}
        </div>
      </div>

      {/* Chat bubbles */}
      <ChatBubble text="Hi! How can I help?" top={50} right={-20} delay={0} />
      <ChatBubble text="Book appointment ✓" top={340} right={-30} delay={1.2} ai={false} />
    </div>
  );
}

function ChatBubble({ text, top, right, delay, ai = true }) {
  return (
    <div style={{
      position: "absolute", top, right,
      background: ai ? "linear-gradient(135deg,rgba(14,113,235,.9),rgba(45,140,255,.9))" : "rgba(9,20,40,.95)",
      border: ai ? "none" : "1px solid var(--border)",
      borderRadius: ai ? "16px 16px 4px 16px" : "16px 16px 16px 4px",
      padding: "10px 16px",
      fontSize: ".82rem", color: "#fff", fontWeight: 500,
      whiteSpace: "nowrap",
      boxShadow: "0 8px 24px rgba(0,0,0,.3)",
      animation: `fadeUp .6s ease ${delay}s both`,
    }}>
      {text}
    </div>
  );
}

// ─── Problem ─────────────────────────────────────────────────────────────────
function Problem() {
  const problems = [
    { icon: <Icon.Phone />, title: "Missed Calls", desc: "Calls go unanswered during peak hours and after-hours, leading to lost revenue and frustrated customers.", color: "#ef4444" },
    { icon: <Icon.Clock />, title: "Delayed Responses", desc: "Long wait times damage brand perception. Customers expect instant answers — not a ticket queue.", color: "#f97316" },
    { icon: <Icon.Users />, title: "Overloaded Teams", desc: "Repetitive queries drain agent productivity, causing burnout and inability to handle complex issues.", color: "#eab308" },
  ];

  return (
    <section id="problem" style={{ padding: "100px 0", position: "relative" }}>
      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px" }}>
        <AnimSection>
          <div style={{ textAlign: "center", marginBottom: 64 }}>
            <div className="section-tag">The Problem</div>
            <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2rem,3.5vw,3rem)", fontWeight: 800, letterSpacing: "-.03em" }}>
              Is Your Support Team<br/>
              <span className="grad-text">Holding You Back?</span>
            </h2>
            <p style={{ color: "var(--muted)", fontSize: "1.05rem", marginTop: 16, maxWidth: 540, margin: "16px auto 0" }}>
              Traditional customer support can't keep pace with modern expectations. Here's what businesses face daily.
            </p>
          </div>
        </AnimSection>

        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(280px,1fr))", gap: 24 }}>
          {problems.map(({ icon, title, desc, color }, i) => (
            <AnimSection key={title} delay={i * 0.12}>
              <div className="glass feat-card" style={{ padding: 36 }}>
                <div style={{
                  width: 56, height: 56, borderRadius: 16, marginBottom: 20,
                  background: `${color}18`, border: `1px solid ${color}33`,
                  display: "flex", alignItems: "center", justifyContent: "center",
                  color,
                }}>
                  {icon}
                </div>
                <h3 style={{ fontFamily: "var(--font-head)", fontSize: "1.25rem", fontWeight: 700, marginBottom: 10 }}>{title}</h3>
                <p style={{ color: "var(--muted)", lineHeight: 1.7, fontSize: ".95rem" }}>{desc}</p>

                {/* Problem indicator bar */}
                <div style={{ marginTop: 20, height: 3, borderRadius: 3, background: "var(--border)" }}>
                  <div style={{ width: `${[85,72,90][i]}%`, height: "100%", borderRadius: 3, background: `linear-gradient(90deg,${color},${color}88)` }}/>
                </div>
                <div style={{ display: "flex", justifyContent: "space-between", marginTop: 6 }}>
                  <span style={{ fontSize: ".72rem", color: "var(--muted)" }}>Impact level</span>
                  <span style={{ fontSize: ".72rem", color }}>{[85,72,90][i]}%</span>
                </div>
              </div>
            </AnimSection>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Solution ─────────────────────────────────────────────────────────────────
function Solution() {
  const items = [
    { icon: <Icon.Bot />,   title: "Chat + Voice AI",        desc: "Deploy conversational AI across chat and voice channels simultaneously — unified experience, zero wait times." },
    { icon: <Icon.Clock />, title: "24/7 Availability",       desc: "Your virtual agent never sleeps. Handle thousands of inquiries around the clock without adding headcount." },
    { icon: <Icon.Globe />, title: "Seamless Integration",    desc: "Plug directly into your existing CRM, ticketing system, and Zoom Contact Center with minimal lift." },
  ];

  return (
    <section id="solution" style={{ padding: "100px 0", position: "relative", background: "var(--navy-2)" }}>
      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px" }}>
        <div style={{ display: "flex", gap: 72, alignItems: "center", flexWrap: "wrap" }}>
          {/* Left */}
          <div style={{ flex: 1, minWidth: 300 }}>
            <AnimSection>
              <div className="section-tag">Our Solution</div>
              <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2rem,3.5vw,3rem)", fontWeight: 800, letterSpacing: "-.03em", marginBottom: 20 }}>
                Sysgrate Integrates<br/>
                <span className="grad-text">Zoom Virtual Agent</span><br/>
                For You
              </h2>
              <p style={{ color: "var(--muted)", fontSize: "1.05rem", lineHeight: 1.8, marginBottom: 36 }}>
                We handle the full implementation — from architecture to go-live — so you can focus on your business while your customers get instant, intelligent support.
              </p>

              <div style={{ display: "flex", flexDirection: "column", gap: 20 }}>
                {items.map(({ icon, title, desc }) => (
                  <div key={title} style={{ display: "flex", gap: 18 }}>
                    <div style={{
                      width: 48, height: 48, flexShrink: 0, borderRadius: 14,
                      background: "linear-gradient(135deg,rgba(14,113,235,.2),rgba(0,194,224,.1))",
                      border: "1px solid var(--border)",
                      display: "flex", alignItems: "center", justifyContent: "center",
                      color: "var(--cyan)",
                    }}>
                      {icon}
                    </div>
                    <div>
                      <h4 style={{ fontFamily: "var(--font-head)", fontWeight: 700, marginBottom: 4 }}>{title}</h4>
                      <p style={{ color: "var(--muted)", fontSize: ".9rem", lineHeight: 1.65 }}>{desc}</p>
                    </div>
                  </div>
                ))}
              </div>
            </AnimSection>
          </div>

          {/* Right: flow diagram */}
          <div style={{ flex: "0 0 420px" }}>
            <AnimSection delay={0.2}>
              <FlowDiagram />
            </AnimSection>
          </div>
        </div>
      </div>
    </section>
  );
}

function FlowDiagram() {
  const steps = [
    { icon: "💬", label: "Customer Reaches Out", sub: "Chat, Voice, or Messaging" },
    { icon: "🤖", label: "Zoom Virtual Agent", sub: "Processes intent with AI" },
    { icon: "⚡", label: "Instant Resolution", sub: "Answers, bookings, escalations" },
    { icon: "📊", label: "Analytics Dashboard", sub: "Insights & performance data" },
  ];
  return (
    <div className="glass" style={{ padding: 36, position: "relative" }}>
      <div style={{ position: "absolute", top: 20, right: 20, display: "flex", gap: 6 }}>
        {["#ef4444","#eab308","#22c55e"].map(c => <div key={c} style={{ width: 10, height: 10, borderRadius: "50%", background: c }}/>)}
      </div>
      <div style={{ fontFamily: "var(--font-head)", fontSize: ".8rem", color: "var(--muted)", marginBottom: 24, fontWeight: 600, letterSpacing: ".08em", textTransform: "uppercase" }}>
        Integration Flow
      </div>
      <div style={{ display: "flex", flexDirection: "column", gap: 0 }}>
        {steps.map(({ icon, label, sub }, i) => (
          <div key={label}>
            <div style={{ display: "flex", gap: 16, alignItems: "flex-start" }}>
              <div style={{
                width: 44, height: 44, flexShrink: 0, borderRadius: 12,
                background: "linear-gradient(135deg,rgba(14,113,235,.25),rgba(0,194,224,.15))",
                border: "1px solid var(--border)",
                display: "flex", alignItems: "center", justifyContent: "center",
                fontSize: "1.2rem",
              }}>
                {icon}
              </div>
              <div style={{ paddingBottom: 4 }}>
                <div style={{ fontWeight: 600, fontSize: ".95rem" }}>{label}</div>
                <div style={{ color: "var(--muted)", fontSize: ".8rem" }}>{sub}</div>
              </div>
            </div>
            {i < steps.length - 1 && (
              <div style={{ marginLeft: 22, width: 2, height: 24, background: "linear-gradient(to bottom, var(--blue), var(--cyan))", opacity: .5, borderRadius: 1 }}/>
            )}
          </div>
        ))}
      </div>

      {/* Bottom status */}
      <div style={{ marginTop: 28, padding: "12px 16px", background: "rgba(0,194,224,.08)", border: "1px solid rgba(0,194,224,.2)", borderRadius: 10 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
          <div style={{ width: 8, height: 8, borderRadius: "50%", background: "#22c55e", boxShadow: "0 0 8px #22c55e" }}/>
          <span style={{ fontSize: ".8rem", color: "var(--cyan)", fontWeight: 600 }}>All systems operational · 99.9% uptime</span>
        </div>
      </div>
    </div>
  );
}

// ─── Features ─────────────────────────────────────────────────────────────────
function Features() {
  const features = [
    { icon: <Icon.Bot />,   title: "AI Chatbot",            desc: "Natural language understanding that resolves queries instantly without human intervention.",  color: "var(--blue)" },
    { icon: <Icon.Mic />,   title: "Voice Automation",      desc: "Intelligent IVR replacement with human-like voice AI that understands context and intent.",   color: "var(--cyan)" },
    { icon: <Icon.Globe />, title: "Omnichannel Support",   desc: "Unified experience across web chat, mobile, WhatsApp, SMS, and voice — all from one platform.", color: "#8b5cf6" },
    { icon: <Icon.Zap />,   title: "Real-time Responses",   desc: "Sub-second response times powered by Zoom's enterprise-grade AI inference infrastructure.", color: "#f59e0b" },
    { icon: <Icon.Chart />, title: "Analytics & Insights",  desc: "Deep dashboards revealing resolution rates, drop-offs, sentiment, and CSAT trends.", color: "#10b981" },
    { icon: <Icon.Shield />,title: "Enterprise Security",   desc: "SOC 2, HIPAA-ready deployment with end-to-end encryption and data residency controls.", color: "#ec4899" },
  ];

  return (
    <section id="features" style={{ padding: "100px 0", position: "relative" }}>
      {/* BG accent */}
      <div style={{ position: "absolute", top: "10%", right: 0, width: 400, height: 400, borderRadius: "50%", background: "radial-gradient(circle, rgba(14,113,235,.12) 0%, transparent 70%)", pointerEvents: "none" }}/>

      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px" }}>
        <AnimSection>
          <div style={{ textAlign: "center", marginBottom: 64 }}>
            <div className="section-tag">Platform Features</div>
            <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2rem,3.5vw,3rem)", fontWeight: 800, letterSpacing: "-.03em" }}>
              Everything You Need to<br/>
              <span className="grad-text">Automate Support</span>
            </h2>
          </div>
        </AnimSection>

        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(300px,1fr))", gap: 24 }}>
          {features.map(({ icon, title, desc, color }, i) => (
            <AnimSection key={title} delay={i * 0.08}>
              <div className="glass feat-card" style={{ padding: 32 }}>
                <div style={{
                  width: 52, height: 52, borderRadius: 14, marginBottom: 20,
                  background: `${color}18`,
                  border: `1px solid ${color}30`,
                  display: "flex", alignItems: "center", justifyContent: "center",
                  color,
                }}>
                  {icon}
                </div>
                <h3 style={{ fontFamily: "var(--font-head)", fontSize: "1.1rem", fontWeight: 700, marginBottom: 8 }}>{title}</h3>
                <p style={{ color: "var(--muted)", fontSize: ".9rem", lineHeight: 1.7 }}>{desc}</p>
              </div>
            </AnimSection>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Benefits ─────────────────────────────────────────────────────────────────
function Benefits() {
  const benefits = [
    { icon: <Icon.Dollar />, num: "40%",  label: "Cost Reduction",         desc: "Cut support operational expenses significantly by automating repetitive tier-1 queries." },
    { icon: <Icon.Zap />,    num: "10×",  label: "Faster Response Time",   desc: "From minutes to milliseconds. Customers get answers before frustration sets in." },
    { icon: <Icon.Smile />,  num: "94%",  label: "Customer Satisfaction",  desc: "CSAT scores soar when customers get instant, accurate, consistent service 24/7." },
    { icon: <Icon.Scale />,  num: "∞",    label: "Infinite Scalability",   desc: "Handle 1 or 100,000 conversations simultaneously — with zero performance degradation." },
  ];

  return (
    <section id="benefits" style={{ padding: "100px 0", background: "var(--navy-2)", position: "relative" }}>
      {/* Decorative line */}
      <div style={{ position: "absolute", top: 0, left: 0, right: 0, height: 1, background: "linear-gradient(90deg,transparent,var(--blue),var(--cyan),transparent)" }}/>

      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px" }}>
        <AnimSection>
          <div style={{ textAlign: "center", marginBottom: 72 }}>
            <div className="section-tag">Measurable Impact</div>
            <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2rem,3.5vw,3rem)", fontWeight: 800, letterSpacing: "-.03em" }}>
              Results That Move<br/>
              <span className="grad-text">The Bottom Line</span>
            </h2>
          </div>
        </AnimSection>

        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(240px,1fr))", gap: 2, border: "1px solid var(--border)", borderRadius: 20, overflow: "hidden" }}>
          {benefits.map(({ icon, num, label, desc }, i) => (
            <AnimSection key={label} delay={i * 0.1}>
              <div style={{
                padding: "44px 32px", textAlign: "center",
                background: "rgba(9,20,40,.7)", borderRight: i < 3 ? "1px solid var(--border)" : "none",
                transition: "background .25s",
                cursor: "default",
              }}
                onMouseEnter={e => e.currentTarget.style.background = "rgba(14,113,235,.1)"}
                onMouseLeave={e => e.currentTarget.style.background = "rgba(9,20,40,.7)"}
              >
                <div style={{ color: "var(--cyan)", marginBottom: 16 }}>{icon}</div>
                <div className="stat-num" style={{ marginBottom: 8 }}>{num}</div>
                <div style={{ fontFamily: "var(--font-head)", fontWeight: 700, fontSize: "1rem", marginBottom: 10 }}>{label}</div>
                <p style={{ color: "var(--muted)", fontSize: ".85rem", lineHeight: 1.65 }}>{desc}</p>
              </div>
            </AnimSection>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Use Cases ────────────────────────────────────────────────────────────────
function UseCases() {
  const cases = [
    { icon: <Icon.Headset />,   title: "Customer Support",       desc: "Automate tier-1 queries, live escalation, and post-resolution follow-up seamlessly.", tag: "Most Popular" },
    { icon: <Icon.Calendar />,  title: "Appointment Booking",    desc: "Let customers self-schedule, reschedule, and receive reminders without calling in.", tag: "" },
    { icon: <Icon.HelpCircle />,title: "FAQ Automation",         desc: "Resolve 80% of common questions instantly with contextual, accurate AI answers.", tag: "" },
    { icon: <Icon.Target />,    title: "Lead Qualification",     desc: "Capture, score, and route leads 24/7 before your sales team even starts their day.", tag: "High ROI" },
  ];

  return (
    <section id="usecases" style={{ padding: "100px 0", position: "relative" }}>
      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px" }}>
        <AnimSection>
          <div style={{ textAlign: "center", marginBottom: 64 }}>
            <div className="section-tag">Use Cases</div>
            <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2rem,3.5vw,3rem)", fontWeight: 800, letterSpacing: "-.03em" }}>
              Automation Built for<br/>
              <span className="grad-text">Real Business Scenarios</span>
            </h2>
          </div>
        </AnimSection>

        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(260px,1fr))", gap: 24 }}>
          {cases.map(({ icon, title, desc, tag }, i) => (
            <AnimSection key={title} delay={i * 0.1}>
              <div className="glass feat-card" style={{ padding: 32, position: "relative" }}>
                {tag && (
                  <div style={{
                    position: "absolute", top: -1, right: 20,
                    background: "linear-gradient(135deg,var(--blue),var(--cyan))",
                    borderRadius: "0 0 8px 8px", padding: "3px 12px",
                    fontSize: ".7rem", fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase",
                  }}>
                    {tag}
                  </div>
                )}
                <div style={{ color: "var(--cyan)", marginBottom: 18 }}>{icon}</div>
                <h3 style={{ fontFamily: "var(--font-head)", fontSize: "1.15rem", fontWeight: 700, marginBottom: 10 }}>{title}</h3>
                <p style={{ color: "var(--muted)", fontSize: ".9rem", lineHeight: 1.7, marginBottom: 20 }}>{desc}</p>

                <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
                  {[
                    ["Intent detection & routing", "Secure data handling", "Zoom Contact Center sync"],
                    ["Multi-timezone support", "Calendar API integration", "Automated reminders"],
                    ["Dynamic knowledge base", "Smart fallback handling", "Regular content updates"],
                    ["Behavioral scoring", "CRM auto-population", "Sales team handoff"],
                  ][i].map(point => (
                    <div key={point} style={{ display: "flex", gap: 8, alignItems: "center" }}>
                      <div style={{ color: "var(--cyan)", flexShrink: 0 }}><Icon.Check /></div>
                      <span style={{ fontSize: ".82rem", color: "var(--muted)" }}>{point}</span>
                    </div>
                  ))}
                </div>
              </div>
            </AnimSection>
          ))}
        </div>
      </div>
    </section>
  );
}

// ─── Why Sysgrate ─────────────────────────────────────────────────────────────
function WhySysgrate() {
  const reasons = [
    { icon: <Icon.Shield />, title: "Zoom Integration Experts",  desc: "Deep expertise as Zoom's technology partner with certified engineers who have deployed solutions globally." },
    { icon: <Icon.Rocket />, title: "Fast Deployment",          desc: "Go live in weeks, not months. Our accelerated onboarding gets your virtual agent answering queries fast." },
    { icon: <Icon.Bot />,    title: "Fully Custom Solutions",   desc: "Every workflow, persona, and integration tailored specifically to your brand and business processes." },
    { icon: <Icon.Headset />,title: "Dedicated Support",        desc: "A committed team alongside you post-launch — monitoring, optimizing, and evolving your AI deployment." },
  ];

  return (
    <section id="why" style={{ padding: "100px 0", background: "var(--navy-2)", position: "relative", overflow: "hidden" }}>
      {/* Glow BG */}
      <div style={{ position: "absolute", bottom: "-20%", left: "10%", width: 500, height: 500, borderRadius: "50%", background: "radial-gradient(circle, rgba(0,194,224,.1) 0%, transparent 70%)", pointerEvents: "none" }}/>

      <div style={{ maxWidth: 1200, margin: "0 auto", padding: "0 32px" }}>
        <div style={{ display: "flex", gap: 80, alignItems: "flex-start", flexWrap: "wrap" }}>
          {/* Left headline */}
          <div style={{ flex: 1, minWidth: 280 }}>
            <AnimSection>
              <div className="section-tag">Why Sysgrate</div>
              <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2rem,3.5vw,3rem)", fontWeight: 800, letterSpacing: "-.03em", marginBottom: 24 }}>
                The Integration<br/>
                Partner You<br/>
                <span className="grad-text">Can Count On</span>
              </h2>
              <p style={{ color: "var(--muted)", lineHeight: 1.8, marginBottom: 36 }}>
                We don't just deploy software — we become an extension of your team, ensuring your Zoom Virtual Agent delivers ROI from day one.
              </p>

              {/* Client logos placeholder */}
              <div style={{ display: "flex", gap: 12, flexWrap: "wrap", marginTop: 8 }}>
                {["Finance", "Healthcare", "Retail", "SaaS", "Telecom"].map(sector => (
                  <div key={sector} style={{ padding: "6px 16px", border: "1px solid var(--border)", borderRadius: 50, fontSize: ".78rem", color: "var(--muted)", fontWeight: 600 }}>
                    {sector}
                  </div>
                ))}
              </div>
            </AnimSection>
          </div>

          {/* Right grid */}
          <div style={{ flex: 1, minWidth: 300, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 20 }}>
            {reasons.map(({ icon, title, desc }, i) => (
              <AnimSection key={title} delay={i * 0.1}>
                <div className="glass feat-card" style={{ padding: 28 }}>
                  <div style={{ color: "var(--cyan)", marginBottom: 14 }}>{icon}</div>
                  <h4 style={{ fontFamily: "var(--font-head)", fontWeight: 700, fontSize: "1rem", marginBottom: 8 }}>{title}</h4>
                  <p style={{ color: "var(--muted)", fontSize: ".83rem", lineHeight: 1.65 }}>{desc}</p>
                </div>
              </AnimSection>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}

// ─── CTA ─────────────────────────────────────────────────────────────────────
function CTA() {
  return (
    <section id="cta" style={{ padding: "120px 32px", position: "relative", overflow: "hidden", textAlign: "center" }}>
      {/* Animated gradient BG */}
      <div style={{
        position: "absolute", inset: 0, zIndex: 0,
        background: "linear-gradient(135deg, var(--navy-3) 0%, var(--navy) 40%, rgba(0,194,224,.08) 100%)",
        backgroundSize: "400% 400%",
        animation: "gradMove 8s ease infinite",
      }}/>
      {/* Grid */}
      <div className="grid-bg" style={{ position: "absolute", inset: 0, opacity: .25 }}/>
      {/* Glows */}
      <div style={{ position: "absolute", top: "50%", left: "50%", transform: "translate(-50%,-50%)", width: 600, height: 300, borderRadius: "50%", background: "radial-gradient(ellipse, rgba(14,113,235,.2) 0%, transparent 70%)", pointerEvents: "none" }}/>

      <div style={{ position: "relative", zIndex: 1, maxWidth: 720, margin: "0 auto" }}>
        <AnimSection>
          <div className="section-tag" style={{ margin: "0 auto 24px" }}>Get Started Today</div>
          <h2 style={{ fontFamily: "var(--font-head)", fontSize: "clamp(2.2rem,5vw,4rem)", fontWeight: 800, letterSpacing: "-.04em", lineHeight: 1.1, marginBottom: 20 }}>
            Smarter Conversations.<br/>
            <span className="shimmer-text">Faster Growth.</span>
          </h2>
          <p style={{ color: "var(--muted)", fontSize: "1.1rem", lineHeight: 1.75, marginBottom: 48, maxWidth: 520, margin: "0 auto 48px" }}>
            Join hundreds of businesses that have transformed their customer experience with Sysgrate and Zoom Virtual Agent. Your automation journey starts with one conversation.
          </p>

          <div style={{ display: "flex", gap: 16, justifyContent: "center", flexWrap: "wrap" }}>
            <a href="mailto:info@sysgrate.com" className="btn-primary" style={{ fontSize: "1.05rem", padding: "16px 40px" }}>
              Schedule a Demo <Icon.Arrow />
            </a>
            <a href="tel:+1-800-SYSGRATE" className="btn-ghost">
              Talk to Sales
            </a>
          </div>

          {/* Trust signals */}
          <div style={{ display: "flex", gap: 32, justifyContent: "center", marginTop: 52, flexWrap: "wrap" }}>
            {["No credit card required", "Setup in 2 weeks", "Enterprise-grade SLA"].map(t => (
              <div key={t} style={{ display: "flex", gap: 8, alignItems: "center" }}>
                <div style={{ color: "#22c55e" }}><Icon.Check /></div>
                <span style={{ fontSize: ".85rem", color: "var(--muted)" }}>{t}</span>
              </div>
            ))}
          </div>
        </AnimSection>
      </div>
    </section>
  );
}

// ─── Footer ───────────────────────────────────────────────────────────────────
function Footer() {
  const links = {
    "Solutions": ["Zoom Virtual Agent", "Voice AI", "Chat Automation", "Omnichannel"],
    "Company": ["About Us", "Careers", "Partners", "Press"],
    "Resources": ["Documentation", "Case Studies", "Blog", "Webinars"],
    "Legal": ["Privacy Policy", "Terms of Service", "Cookie Policy", "GDPR"],
  };

  return (
    <footer style={{ background: "var(--navy-2)", borderTop: "1px solid var(--border)", padding: "72px 32px 36px" }}>
      <div style={{ maxWidth: 1200, margin: "0 auto" }}>
        <div style={{ display: "grid", gridTemplateColumns: "2fr 1fr 1fr 1fr 1fr", gap: 40, marginBottom: 60, flexWrap: "wrap" }}>
          {/* Brand */}
          <div>
            <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 16 }}>
              <div style={{ width: 36, height: 36, borderRadius: 10, background: "linear-gradient(135deg,var(--blue),var(--cyan))", display: "flex", alignItems: "center", justifyContent: "center", boxShadow: "0 0 18px rgba(0,194,224,.4)" }}>
                <svg width="18" height="18" fill="none" viewBox="0 0 24 24" stroke="white" strokeWidth="2"><path strokeLinecap="round" strokeLinejoin="round" d="M13 10V3L4 14h7v7l9-11h-7z"/></svg>
              </div>
              <span style={{ fontFamily: "var(--font-head)", fontWeight: 800, fontSize: "1.2rem" }}>Sys<span className="grad-text">grate</span></span>
            </div>
            <p style={{ color: "var(--muted)", fontSize: ".88rem", lineHeight: 1.7, maxWidth: 240, marginBottom: 20 }}>
              Your trusted partner for Zoom Virtual Agent and Voice AI integration services.
            </p>

            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              <a href="mailto:info@sysgrate.com" style={{ display: "flex", gap: 8, alignItems: "center", color: "var(--muted)", textDecoration: "none", fontSize: ".85rem", transition: "color .2s" }}
                 onMouseEnter={e => e.currentTarget.style.color = "var(--white)"}
                 onMouseLeave={e => e.currentTarget.style.color = "var(--muted)"}>
                <Icon.Mail /> info@sysgrate.com
              </a>
              <a href="tel:+918000000000" style={{ display: "flex", gap: 8, alignItems: "center", color: "var(--muted)", textDecoration: "none", fontSize: ".85rem", transition: "color .2s" }}
                 onMouseEnter={e => e.currentTarget.style.color = "var(--white)"}
                 onMouseLeave={e => e.currentTarget.style.color = "var(--muted)"}>
                <Icon.Phone /> +91 80 0000 0000
              </a>
            </div>

            {/* Social */}
            <div style={{ display: "flex", gap: 12, marginTop: 20 }}>
              {[<Icon.Linkedin />, <Icon.Twitter />].map((ico, i) => (
                <a key={i} href="#" style={{
                  width: 36, height: 36, borderRadius: 10,
                  background: "rgba(255,255,255,.06)", border: "1px solid var(--border)",
                  display: "flex", alignItems: "center", justifyContent: "center",
                  color: "var(--muted)", transition: "color .2s, border-color .2s, background .2s", textDecoration: "none",
                }}
                  onMouseEnter={e => { e.currentTarget.style.color = "var(--cyan)"; e.currentTarget.style.borderColor = "rgba(0,194,224,.4)"; e.currentTarget.style.background = "rgba(0,194,224,.1)"; }}
                  onMouseLeave={e => { e.currentTarget.style.color = "var(--muted)"; e.currentTarget.style.borderColor = "var(--border)"; e.currentTarget.style.background = "rgba(255,255,255,.06)"; }}>
                  {ico}
                </a>
              ))}
            </div>
          </div>

          {/* Link columns */}
          {Object.entries(links).map(([heading, items]) => (
            <div key={heading}>
              <div style={{ fontFamily: "var(--font-head)", fontWeight: 700, fontSize: ".85rem", letterSpacing: ".08em", textTransform: "uppercase", color: "var(--white)", marginBottom: 16 }}>
                {heading}
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
                {items.map(item => (
                  <a key={item} href="#" style={{ color: "var(--muted)", textDecoration: "none", fontSize: ".88rem", transition: "color .2s" }}
                     onMouseEnter={e => e.currentTarget.style.color = "var(--white)"}
                     onMouseLeave={e => e.currentTarget.style.color = "var(--muted)"}>
                    {item}
                  </a>
                ))}
              </div>
            </div>
          ))}
        </div>

        {/* Bottom bar */}
        <div style={{ borderTop: "1px solid var(--border)", paddingTop: 28, display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: 16 }}>
          <div style={{ fontSize: ".82rem", color: "var(--muted)" }}>
            © 2025 Sysgrate Technologies Pvt. Ltd. · All rights reserved.
          </div>
          <div style={{ fontSize: ".78rem", color: "var(--muted)", display: "flex", alignItems: "center", gap: 6 }}>
            <div style={{ width: 6, height: 6, borderRadius: "50%", background: "var(--blue-2)" }}/>
            Official Zoom Technology Partner
          </div>
        </div>
      </div>
    </footer>
  );
}

// ─── Root App ─────────────────────────────────────────────────────────────────
export default function App() {
  return (
    <>
      <InjectCSS />
      <div className="noise">
        <Navbar />
        <Hero />
        <div className="divider" />
        <Problem />
        <div className="divider" />
        <Solution />
        <div className="divider" />
        <Features />
        <div className="divider" />
        <Benefits />
        <div className="divider" />
        <UseCases />
        <div className="divider" />
        <WhySysgrate />
        <div className="divider" />
        <CTA />
        <Footer />
      </div>
    </>
  );
}
